import React, { Component } from 'react'
import { View, Text } from 'react-native'
import { Actions } from 'react-native-router-flux'

export default class GraphScene extends Component {
  render() {
    return (
      <Text>GraphScene</Text>
    )
  }
}
